from setuptools import setup, find_packages

setup(
    name='toon_maker',
    version='1.0.0',
    packages=find_packages(),
    install_requires=[],
    description='Normal to Master to TOON Format Converter',
    author='ZenleX-Dost',
    python_requires='>=3.7',
)
